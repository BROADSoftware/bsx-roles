127.0.0.1   localhost localhost.localdomain localhost4 localhost4.localdomain4
::1         localhost localhost.localdomain localhost6 localhost6.localdomain6

{% for host in groups['all']  %}
{% if hostvars[host].ansible_default_ipv4 is defined and hostvars[host].ansible_default_ipv4.address is defined %}
{{ hostvars[host].ansible_default_ipv4.address }} {% if cluster_domain is defined %}{{ hostvars[host].ansible_hostname }}.{{ cluster_domain }} {% endif %}{{ hostvars[host].ansible_hostname }}
{% endif %}
{% endfor %}

