# ANSIBLE GENERATED.
{% if ifcfg is defined and ifcfg.routes is defined and (ifcfg.enabled is undefined or ifcfg.enabled) %}
{% for i in ifcfg.routes %}
ADDRESS{{ loop.index - 1  }}={{ i.network }}
NETMASK{{ loop.index - 1 }}={{ i.netmask }}
GATEWAY{{ loop.index - 1 }}={{ i.gateway }}
{% endfor %}
{% endif %}

